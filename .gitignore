# secrets
.env
.env.*
!.env.example

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# node
node_modules/
client/dist/
bun.lockb

# data + scratch
#
# Anchored to the repo root on purpose. An unanchored "data/" also matches
# pipeline/data/, which holds vendored source data that MUST be committed --
# and the failure is silent locally, because the files are there on disk.
/data/
/scratch/

# local agent state
.claude/settings.local.json

# os
.DS_Store
